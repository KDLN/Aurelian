
name: deploy-realtime-cloudrun
on:
  push:
    branches: [ master ]
    paths:
      - 'apps/realtime/**'
      - 'prisma/**'
      - '.github/workflows/realtime-cloudrun.yml'
jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions: { contents: read, id-token: write }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}
          project_id: ${{ secrets.GCP_PROJECT_ID }}
      - uses: google-github-actions/setup-gcloud@v2
        with: { version: '>= 471.0.0' }
      - run: gcloud auth configure-docker ${{ secrets.GCP_REGION }}-docker.pkg.dev
      - name: Generate Prisma Client
        run: |
          npm install
          npx prisma generate
      - name: Run database migrations
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          DIRECT_URL: ${{ secrets.DIRECT_URL }}
        run: |
          echo "Running Prisma migrations..."
          npx prisma migrate deploy
          echo "✓ Migrations completed successfully"
      - name: Build & push optimized image
        env:
          REGION: ${{ secrets.GCP_REGION }}
          PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
        run: |
          IMAGE_URI="$REGION-docker.pkg.dev/$PROJECT_ID/aurelian/realtime:${GITHUB_SHA::7}"
          
          # Build with BuildKit for better caching and smaller images
          DOCKER_BUILDKIT=1 docker build \
            -f apps/realtime/Dockerfile \
            --target runner \
            --cache-from "$REGION-docker.pkg.dev/$PROJECT_ID/aurelian/realtime:cache" \
            --build-arg BUILDKIT_INLINE_CACHE=1 \
            -t "$IMAGE_URI" \
            -t "$REGION-docker.pkg.dev/$PROJECT_ID/aurelian/realtime:cache" \
            .
          
          docker push "$IMAGE_URI"
          docker push "$REGION-docker.pkg.dev/$PROJECT_ID/aurelian/realtime:cache"
          echo "IMAGE_URI=$IMAGE_URI" >> $GITHUB_ENV
          
      - name: Deploy with optimized configuration
        env:
          REGION: ${{ secrets.GCP_REGION }}
          SERVICE: ${{ secrets.REALTIME_SERVICE }}
          IMAGE_URI: ${{ env.IMAGE_URI }}
        run: |
          gcloud run deploy "$SERVICE" \
            --image "$IMAGE_URI" \
            --region "$REGION" \
            --allow-unauthenticated \
            --port=8787 \
            --min-instances=1 \
            --max-instances=10 \
            --memory=512Mi \
            --cpu=1 \
            --concurrency=200 \
            --timeout=300s \
            --set-env-vars="NODE_ENV=production,DATABASE_POOL_SIZE=30,DATABASE_POOL_TIMEOUT=15" \
            --execution-environment=gen2
